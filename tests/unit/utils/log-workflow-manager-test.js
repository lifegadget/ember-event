import logWorkflowManager from 'dummy/utils/log-workflow-manager';
import { module, test } from 'qunit';

module('Unit | Utility | log workflow manager');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logWorkflowManager();
  assert.ok(result);
});
